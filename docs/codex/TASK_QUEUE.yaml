meta:
  version: 1
  branch_policy: "work only on codex/* branches"
  task_selection_rule: "pick the highest-priority task with status pending and no incomplete dependencies"
  commit_policy: "complete one closed-loop task, validate, update docs, commit, push"
  current_focus: "Queue clear after BJ1: no pending tasks remain; add the next prioritized task before the next Codex run"

tasks:
  - id: A0
    title: "Bootstrap Codex operating model"
    priority: 1
    status: completed
    depends_on: []
    area: "repo-operations"
    problem_statement: "Codex does not yet have durable repo-level memory, task selection, or acceptance criteria inside the repository."
    root_cause_hypothesis: "Project instructions, architecture context, and acceptance standards are still conversational instead of repo-native."
    definition_of_done:
      - "AGENTS.md points to docs/codex as the source of truth"
      - "PROJECT_BRIEF, SYSTEM_MAP, CONTRACTS, QUALITY_GATES, DECISIONS, RUNBOOK, and TASK_QUEUE exist"
      - "A baseline CI workflow and local quality script exist"
    validation_commands:
      - "python scripts/quality_gate.py --profile python"
    risk_level: "medium"
    expected_files:
      - "AGENTS.md"
      - "docs/codex/*"
      - "scripts/quality_gate.py"
      - ".github/workflows/quality-gates.yml"
    notes:
      - "Bootstrap task completed by codex/lightrag operating model setup."

  - id: A1
    title: "Absorb active worktree into a documented engineering baseline"
    priority: 2
    status: completed
    depends_on: [A0]
    area: "baseline"
    problem_statement: "The branch already contains local changes, but they are not yet summarized as the active engineering baseline."
    root_cause_hypothesis: "The repo lacks a single documented snapshot of what changed before the larger optimization work begins."
    definition_of_done:
      - "Current local modifications are summarized in DECISIONS or a dedicated baseline note"
      - "Bootstrap changes are committed separately from unrelated product changes"
      - "The next task queue reflects the real baseline"
    validation_commands:
      - "git status --short --branch"
    risk_level: "low"
    expected_files:
      - "docs/codex/DECISIONS.md"
      - "docs/codex/TASK_QUEUE.yaml"
    notes:
      - "Completed by recording the active storage-stack consolidation baseline in DCR-005."
      - "Bootstrap commits remain isolated in history while the dirty worktree is treated as the current product baseline."

  - id: B1
    title: "Split lightrag/lightrag.py by orchestration responsibility"
    priority: 3
    status: completed
    depends_on: [A1]
    area: "core-engine"
    problem_statement: "The main LightRAG orchestration file is too large and mixes multiple responsibilities."
    root_cause_hypothesis: "Configuration, orchestration, storage wiring, and query flow evolved in one file without clear seams."
    definition_of_done:
      - "A smaller orchestration entrypoint remains"
      - "At least one responsibility is extracted into a dedicated module with tests"
      - "Contracts and migration notes are updated if public imports change"
    validation_commands:
      - "python -m ruff check lightrag/lightrag.py lightrag/storage_wiring.py tests/test_storage_wiring.py"
      - "python -m pytest tests/test_chunking.py tests/test_write_json_optimization.py tests/test_storage_wiring.py -m \"not integration\""
    risk_level: "high"
    expected_files:
      - "lightrag/lightrag.py"
      - "lightrag/*"
      - "tests/*"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Prefer extraction over alias-heavy compatibility wrappers."
      - "Completed by extracting storage class resolution and namespace wiring into lightrag/storage_wiring.py."
      - "Focused regression coverage lives in tests/test_storage_wiring.py."
      - "Validation is scoped to the touched seam because repo-wide Ruff still reports unrelated baseline lint debt outside B1."

  - id: B2
    title: "Split lightrag/operate.py into retrieval and indexing modules"
    priority: 4
    status: completed
    depends_on: [B1]
    area: "core-engine"
    problem_statement: "The operate module contains chunking, extraction, retrieval, and graph rebuild behavior in one place."
    root_cause_hypothesis: "Pipeline steps accumulated without intermediate module boundaries."
    definition_of_done:
      - "Retrieval and indexing concerns live in separate modules"
      - "Shared helpers move to explicit utility modules or protocols"
      - "Touched behavior is covered by focused tests"
    validation_commands:
      - "python -m ruff check lightrag/operate.py lightrag/indexing.py lightrag/retrieval.py lightrag/lightrag.py tests/test_chunking.py tests/test_operate_split.py"
      - "python -m pytest tests/test_chunking.py tests/test_write_json_optimization.py tests/test_operate_split.py -m \"not integration\""
    risk_level: "high"
    expected_files:
      - "lightrag/operate.py"
      - "lightrag/*"
      - "tests/*"
    notes:
      - "Completed by moving indexing/chunking concerns into lightrag/indexing.py and retrieval/query concerns into lightrag/retrieval.py."
      - "lightrag/operate.py remains a thin compatibility facade so existing imports stay stable."
      - "Validation is scoped to the touched seam because repo-wide Ruff still reports unrelated baseline lint debt outside B2."

  - id: C1
    title: "Define shared storage adapter contracts"
    priority: 5
    status: completed
    depends_on: [B2]
    area: "storage"
    problem_statement: "Storage implementations diverge in initialization, error handling, and capability expression."
    root_cause_hypothesis: "Adapters were added incrementally without a strict shared contract."
    definition_of_done:
      - "Common adapter expectations are explicit"
      - "At least one contract-focused test suite exists for touched adapters"
      - "Provider-specific duplication is reduced"
    validation_commands:
      - "python -m ruff check lightrag/kg/storage_contracts.py lightrag/kg/chroma_impl.py lightrag/kg/postgres_impl.py lightrag/kg/neo4j_impl.py tests/test_storage_contracts.py"
      - "python -m pytest tests/test_chroma_storage.py tests/test_storage_contracts.py -m \"not integration\""
    risk_level: "high"
    expected_files:
      - "lightrag/kg/*"
      - "tests/*"
      - "docs/codex/CONTRACTS.md"
    notes:
      - "Target protocol clarity before broad implementation churn."
      - "Completed by extracting shared workspace resolution into lightrag/kg/storage_contracts.py and rewiring PostgreSQL, Chroma, and Neo4j to use explicit precedence chains."
      - "Focused contract coverage lives in tests/test_storage_contracts.py, with tests/test_chroma_storage.py kept green as adapter regression coverage."
      - "Validation is scoped to the touched adapter seam because repo-wide Ruff still reports unrelated baseline lint debt outside C1."

  - id: D1
    title: "Split API app factory and route composition"
    priority: 6
    status: completed
    depends_on: [C1]
    area: "api"
    problem_statement: "The FastAPI entrypoint mixes startup concerns, config handling, and route composition."
    root_cause_hypothesis: "App composition grew inside a single module."
    definition_of_done:
      - "App factory and route registration live in separate modules"
      - "Auth and dependency setup are clearer"
      - "Contracts reflect the new structure"
    validation_commands:
      - "python -m ruff check lightrag/api/lightrag_server.py lightrag/api/route_registry.py tests/test_route_registry.py"
      - "python -m pytest tests/test_route_registry.py -q"
    risk_level: "high"
    expected_files:
      - "lightrag/api/lightrag_server.py"
      - "lightrag/api/*"
      - "docs/codex/CONTRACTS.md"
    notes:
      - "Protect externally visible route paths unless a migration is recorded."
      - "Completed by extracting route assembly, auth/status endpoints, health route registration, and static mounts into lightrag/api/route_registry.py while keeping create_app() in lightrag/api/lightrag_server.py."
      - "Focused API coverage lives in tests/test_route_registry.py."
      - "Validation is scoped to the touched API seam because repo-wide Ruff still reports unrelated baseline lint debt outside D1."

  - id: D2
    title: "Trim Web UI to core user journeys"
    priority: 7
    status: completed
    depends_on: [D1]
    area: "webui"
    problem_statement: "The Web UI needs clearer ownership of state and stronger focus on the core product flows."
    root_cause_hypothesis: "Feature growth happened without a formal simplification pass."
    definition_of_done:
      - "Primary tabs and state flows are documented and simplified"
      - "Unused or redundant UI paths are removed"
      - "The app still builds cleanly"
    validation_commands:
      - "bun install --cwd lightrag_webui --frozen-lockfile"
      - "bun --cwd lightrag_webui run build"
    risk_level: "medium"
    expected_files:
      - "lightrag_webui/src/*"
      - "docs/codex/CONTRACTS.md"
    notes:
      - "Prefer user-journey clarity over sweeping visual redesign."
      - "Completed by centralizing the four primary tabs in lightrag_webui/src/lib/appTabs.ts, making the persisted currentTab store the single source of truth for Radix Tabs, and removing the redundant TabVisibility context and unused TabContent path."
      - "ApiSite now relies on the force-mounted Radix tab shell instead of a parallel visibility context, so the Swagger iframe stays warm without extra state ownership."

  - id: E1
    title: "Add performance benchmark harness and final acceptance report"
    priority: 8
    status: completed
    depends_on: [D2]
    area: "performance"
    problem_statement: "The repo does not yet have a durable benchmark and acceptance reporting loop for the optimized architecture."
    root_cause_hypothesis: "Performance checks and structural acceptance are still ad hoc."
    definition_of_done:
      - "Baseline benchmark commands are defined"
      - "Performance results can be compared across iterations"
      - "Final acceptance notes are documented"
    validation_commands:
      - "python scripts/quality_gate.py --profile phase-a"
    risk_level: "medium"
    expected_files:
      - "docs/codex/QUALITY_GATES.md"
      - "docs/codex/DECISIONS.md"
      - "scripts/*"
    notes:
      - "This task closes the optimization program, not just a single code change."
      - "Completed by extending scripts/quality_gate.py so the phase-a profile measures each baseline command, writes `docs/codex/benchmarks/phase_a_latest.json`, appends `docs/codex/benchmarks/phase_a_history.jsonl`, and regenerates `docs/codex/ACCEPTANCE_REPORT.md`."
      - "Validation ran through `python scripts/quality_gate.py --profile phase-a`, which passed with the generated artifacts recorded from the current branch tip."

  - id: F1
    title: "Converge the documented storage stack on Postgres + Chroma + Neo4j"
    priority: 9
    status: completed
    depends_on: [E1]
    area: "storage"
    problem_statement: "Storage defaults, registry validation, deployment docs, and migration guidance drifted apart while the branch converged on a single production storage stack."
    root_cause_hypothesis: "The codebase still treated storage selection as open-ended even after the intended runtime baseline moved to Postgres for documents/status, Chroma for vectors, and Neo4j for graphs."
    definition_of_done:
      - "Default storage selection surfaces point to `PGKVStorage + PGDocStatusStorage + ChromaVectorDBStorage + Neo4JStorage`"
      - "Storage registry and PostgreSQL bootstrap only expose or manage tables for the intended production stack"
      - "Legacy local storage has a documented migration path into the production stack"
      - "Focused regression tests lock the storage-stack contract"
    validation_commands:
      - "python -m ruff check lightrag/api/config.py lightrag/kg/__init__.py lightrag/kg/chroma_impl.py lightrag/kg/postgres_impl.py lightrag/tools/migrate_to_pg_chroma_neo4j.py tests/test_chroma_storage.py tests/test_graph_storage.py tests/test_storage_contracts.py tests/test_storage_stack_defaults.py"
      - "python -m pytest tests/test_chroma_storage.py tests/test_storage_contracts.py tests/test_storage_stack_defaults.py -m \"not integration\""
    risk_level: "high"
    expected_files:
      - "README.md"
      - "README-zh.md"
      - "config.ini.example"
      - "docker-compose.yml"
      - "env.example"
      - "lightrag/api/README.md"
      - "lightrag/api/README-zh.md"
      - "lightrag/api/config.py"
      - "lightrag/kg/__init__.py"
      - "lightrag/kg/postgres_impl.py"
      - "lightrag/tools/migrate_to_pg_chroma_neo4j.py"
      - "pyproject.toml"
      - "requirements-offline*.txt"
      - "tests/*"
      - "docs/codex/CONTRACTS.md"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by making the official storage contract explicit across runtime defaults, storage verification, deployment docs, and migration guidance."
      - "Focused regression coverage lives in tests/test_storage_stack_defaults.py, with tests/test_chroma_storage.py and tests/test_storage_contracts.py covering the touched adapter seam."
      - "Validation is scoped to the touched storage seam because repo-wide Ruff still reports unrelated baseline lint debt outside F1."

  - id: G1
    title: "Centralize pipmaster fallback on the supported runtime surface"
    priority: 10
    status: completed
    depends_on: [F1]
    area: "runtime-dependencies"
    problem_statement: "Supported runtime modules still hard-import `pipmaster`, which leaves source checkouts depending on a local shim when the package is not already installed."
    root_cause_hypothesis: "Optional dependency bootstrap logic evolved independently in API and storage modules, so the supported runtime surface never converged on one shared import boundary for `pipmaster`."
    definition_of_done:
      - "The supported runtime surface uses one shared helper to load `pipmaster` or a local fallback"
      - "The production API and storage modules no longer duplicate inline `pipmaster` import logic"
      - "Focused tests cover the fallback helper behavior"
    validation_commands:
      - "python -m ruff check lightrag/_pipmaster.py lightrag/api/lightrag_server.py lightrag/api/run_with_gunicorn.py lightrag/kg/chroma_impl.py lightrag/kg/neo4j_impl.py lightrag/kg/postgres_impl.py tests/test_pipmaster_fallback.py"
      - "python -m pytest tests/test_pipmaster_fallback.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/_pipmaster.py"
      - "lightrag/api/*"
      - "lightrag/kg/*"
      - "tests/*"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Keep scope on the supported runtime surface defined by the current contracts instead of sweeping every legacy adapter in one run."
      - "Completed by adding lightrag/_pipmaster.py as the shared import-or-install boundary and rewiring the active API, PostgreSQL, Chroma, and Neo4j modules to use it."
      - "Focused regression coverage lives in tests/test_pipmaster_fallback.py."

  - id: H1
    title: "Absorb the repo-root pipmaster shim as a local runtime artifact"
    priority: 11
    status: completed
    depends_on: [G1]
    area: "repo-hygiene"
    problem_statement: "After G1, the only remaining dirty-worktree artifact is an untracked repo-root `pipmaster.py` shim that is no longer part of the supported runtime path."
    root_cause_hypothesis: "The repository had no explicit ignore rule for the temporary root-level compatibility shim, so local runtime recovery artifacts still appeared as unexplained worktree changes."
    definition_of_done:
      - "The repo-root `pipmaster.py` shim is classified as a local artifact rather than pending product work"
      - "The standard worktree status is clean after the ignore rule is in place"
      - "Codex docs record why the ignore rule exists"
    validation_commands:
      - "git check-ignore -v pipmaster.py"
      - "git status --short --branch"
    risk_level: "low"
    expected_files:
      - ".gitignore"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by ignoring the obsolete repo-root pipmaster shim instead of deleting a local file from the user's machine."

  - id: I1
    title: "Extract document status and pagination schemas from document_routes.py"
    priority: 12
    status: completed
    depends_on: [H1]
    area: "api"
    problem_statement: "The document API router still mixes status/pagination schema declarations with document management, parsing, background pipeline, and route handlers in one oversized module."
    root_cause_hypothesis: "As document features grew, Pydantic schema definitions accumulated directly inside document_routes.py instead of converging on a dedicated API-model seam."
    definition_of_done:
      - "Document status and pagination request/response schemas live in a dedicated module"
      - "document_routes.py preserves the existing import surface by reusing the extracted schemas"
      - "Focused tests cover the extracted schema seam and compatibility import path"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/document_routes.py lightrag/api/routers/document_status_models.py tests/test_document_status_models.py"
      - "python -m pytest tests/test_document_status_models.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/document_routes.py"
      - "lightrag/api/routers/document_status_models.py"
      - "tests/test_document_status_models.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving document status, track-status, pagination, and pipeline-status schemas into lightrag/api/routers/document_status_models.py while keeping document_routes.py as the compatibility import surface."

  - id: J1
    title: "Extract document operation schemas from document_routes.py"
    priority: 13
    status: completed
    depends_on: [I1]
    area: "api"
    problem_statement: "The document API router still mixes document write/delete request and response schemas with document management, parsing, background pipeline, and route handlers in one oversized module."
    root_cause_hypothesis: "After status models were extracted, the remaining write/delete schemas still lived directly inside document_routes.py because document API contracts had accumulated around the router instead of converging on dedicated model seams."
    definition_of_done:
      - "Document write/delete request and response schemas live in a dedicated module"
      - "document_routes.py preserves the existing import surface by reusing the extracted schemas"
      - "Focused tests cover the extracted schema seam, validators, and compatibility import path"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/document_routes.py lightrag/api/routers/document_operation_models.py tests/test_document_operation_models.py"
      - "python -m pytest tests/test_document_operation_models.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/document_routes.py"
      - "lightrag/api/routers/document_operation_models.py"
      - "tests/test_document_operation_models.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving document scan, insert, clear-cache, clear-documents, and delete request/response schemas into lightrag/api/routers/document_operation_models.py while keeping document_routes.py as the compatibility import surface."

  - id: K1
    title: "Extract DocumentManager and file-path helpers from document_routes.py"
    priority: 14
    status: completed
    depends_on: [J1]
    area: "api"
    problem_statement: "The document API router still owns the DocumentManager abstraction plus file-path sanitization and queue-filename helpers alongside extraction pipelines and route handlers."
    root_cause_hypothesis: "Document ingestion support code accumulated inside document_routes.py because the router became the default home for anything document-related, even when the logic did not depend on route state."
    definition_of_done:
      - "DocumentManager and its file-path helper functions live in a dedicated module"
      - "document_routes.py preserves the existing import surface by reusing the extracted manager seam"
      - "Focused tests cover workspace isolation, file scanning, path sanitization, and compatibility import paths"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/document_routes.py lightrag/api/routers/document_manager.py tests/test_document_manager.py"
      - "python -m pytest tests/test_document_manager.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/document_routes.py"
      - "lightrag/api/routers/document_manager.py"
      - "tests/test_document_manager.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving DocumentManager, filename sanitization, deletion path validation, and enqueued-file naming into lightrag/api/routers/document_manager.py while keeping document_routes.py as the compatibility import surface."

  - id: L1
    title: "Extract document content extraction helpers from document_routes.py"
    priority: 15
    status: completed
    depends_on: [K1]
    area: "api"
    problem_statement: "The document API router still owns docling availability checks and PDF/DOCX/PPTX/XLSX content extraction helpers alongside queue orchestration and route handlers."
    root_cause_hypothesis: "Document ingestion support kept accumulating in document_routes.py because file-format extraction logic lived next to the enqueue pipeline that called it, even though the extraction code does not depend on route state."
    definition_of_done:
      - "Document format extraction helpers live in a dedicated module"
      - "document_routes.py preserves the existing import surface by reusing the extracted extraction seam"
      - "Focused tests cover the extracted escape helpers and compatibility import path"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/document_routes.py lightrag/api/routers/document_content_extraction.py tests/test_document_content_extraction.py"
      - "python -m pytest tests/test_document_content_extraction.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/document_routes.py"
      - "lightrag/api/routers/document_content_extraction.py"
      - "tests/test_document_content_extraction.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving docling availability checks plus PDF, DOCX, PPTX, and XLSX content extraction helpers into lightrag/api/routers/document_content_extraction.py while keeping document_routes.py as the compatibility import surface."

  - id: M1
    title: "Extract document indexing pipeline helpers from document_routes.py"
    priority: 16
    status: completed
    depends_on: [L1]
    area: "api"
    problem_statement: "The document API router still owns file enqueueing, sequential indexing, text indexing, and directory scan orchestration alongside route handlers and deletion flows."
    root_cause_hypothesis: "Document indexing behavior kept growing inside document_routes.py because the background tasks were defined next to the endpoints that schedule them, even though the indexing pipeline does not depend on route state."
    definition_of_done:
      - "Document indexing pipeline helpers live in a dedicated module"
      - "document_routes.py preserves the existing import surface by reusing the extracted pipeline seam"
      - "Focused tests cover compatibility imports plus the key indexing sort and scan-filter behaviors"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/document_routes.py lightrag/api/routers/document_indexing_pipeline.py tests/test_document_indexing_pipeline.py"
      - "python -m pytest tests/test_document_indexing_pipeline.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/document_routes.py"
      - "lightrag/api/routers/document_indexing_pipeline.py"
      - "tests/test_document_indexing_pipeline.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving file enqueueing, sequential indexing, text indexing, and scan orchestration helpers into lightrag/api/routers/document_indexing_pipeline.py while keeping document_routes.py as the compatibility import surface."

  - id: N1
    title: "Extract document deletion pipeline helpers from document_routes.py"
    priority: 17
    status: completed
    depends_on: [M1]
    area: "api"
    problem_statement: "The document API router still owns the batch document deletion background workflow alongside route handlers and the remaining document-management endpoints."
    root_cause_hypothesis: "Deletion orchestration stayed inline in document_routes.py because the delete endpoint and its background worker evolved together, even though the worker depends on shared pipeline state and filesystem helpers rather than route state."
    definition_of_done:
      - "Document deletion background workflow lives in a dedicated module"
      - "document_routes.py preserves the existing import surface by reusing the extracted deletion seam"
      - "Focused tests cover compatibility imports and the pending-request handoff after deletion completes"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/document_routes.py lightrag/api/routers/document_deletion_pipeline.py tests/test_document_deletion_pipeline.py"
      - "python -m pytest tests/test_document_deletion_pipeline.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/document_routes.py"
      - "lightrag/api/routers/document_deletion_pipeline.py"
      - "tests/test_document_deletion_pipeline.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving the batch document deletion background worker into lightrag/api/routers/document_deletion_pipeline.py while keeping document_routes.py as the compatibility import surface."

  - id: O1
    title: "Extract the clear-documents workflow from document_routes.py"
    priority: 18
    status: completed
    depends_on: [N1]
    area: "api"
    problem_statement: "The document API router still owns the storage-clearing workflow that coordinates pipeline state, storage drop operations, and input-directory cleanup."
    root_cause_hypothesis: "The clear-documents endpoint evolved as a long inline workflow inside document_routes.py because it was triggered directly by a route, even though the logic depends on shared pipeline state and storage adapters rather than route state."
    definition_of_done:
      - "The clear-documents workflow lives in a dedicated pipeline module"
      - "document_routes.py preserves the existing import surface by reusing the extracted clear-documents seam"
      - "Focused tests cover the compatibility import path and the top-level-only input-file cleanup behavior"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/document_routes.py lightrag/api/routers/document_clear_pipeline.py tests/test_document_clear_pipeline.py"
      - "python -m pytest tests/test_document_clear_pipeline.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/document_routes.py"
      - "lightrag/api/routers/document_clear_pipeline.py"
      - "tests/test_document_clear_pipeline.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving the clear-documents storage-and-filesystem workflow into lightrag/api/routers/document_clear_pipeline.py while keeping document_routes.py as the compatibility import surface."

  - id: P1
    title: "Extract document pipeline control helpers from document_routes.py"
    priority: 19
    status: completed
    depends_on: [O1]
    area: "api"
    problem_statement: "The document API router still owns pipeline status reads, failed-document reprocessing startup, and pipeline cancellation logic alongside route definitions."
    root_cause_hypothesis: "Pipeline control behavior accumulated inline in document_routes.py because these endpoints were added next to the routes that trigger them, even though the logic depends on shared pipeline state and background task orchestration rather than route state."
    definition_of_done:
      - "Pipeline status, reprocess, and cancellation helpers live in a dedicated module"
      - "document_routes.py preserves the extracted helper import surface through compatibility aliases"
      - "Focused tests cover helper re-exports plus key pipeline-control behaviors such as history truncation and cancellation flag updates"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/document_routes.py lightrag/api/routers/document_pipeline_control.py tests/test_document_pipeline_control.py"
      - "python -m pytest tests/test_document_pipeline_control.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/document_routes.py"
      - "lightrag/api/routers/document_pipeline_control.py"
      - "tests/test_document_pipeline_control.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving pipeline-status reads, failed-document reprocessing startup, and cancellation logic into lightrag/api/routers/document_pipeline_control.py while keeping document_routes.py as the compatibility import surface."

  - id: Q1
    title: "Extract read-only document status query helpers from document_routes.py"
    priority: 20
    status: completed
    depends_on: [P1]
    area: "api"
    problem_statement: "The document API router still owns read-only document status queries and repeats DocStatusResponse assembly across grouped, tracked, paginated, and aggregated status endpoints."
    root_cause_hypothesis: "Document read endpoints accumulated inline in document_routes.py because they were treated as simple route handlers, even though they share a common response-building seam and do not depend on route state."
    definition_of_done:
      - "Read-only document status query helpers live in a dedicated module"
      - "Shared DocStatusResponse construction is centralized for the extracted query paths"
      - "document_routes.py preserves the helper import surface through compatibility aliases"
      - "Focused tests cover re-exports plus key grouped, tracked, and paginated query behaviors"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/document_routes.py lightrag/api/routers/document_status_queries.py tests/test_document_status_queries.py"
      - "python -m pytest tests/test_document_status_queries.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/document_routes.py"
      - "lightrag/api/routers/document_status_queries.py"
      - "tests/test_document_status_queries.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving grouped, tracked, paginated, and aggregated document status query helpers into lightrag/api/routers/document_status_queries.py while keeping document_routes.py as the compatibility import surface."

  - id: R1
    title: "Extract document mutation command helpers from document_routes.py"
    priority: 21
    status: completed
    depends_on: [Q1]
    area: "api"
    problem_statement: "The document API router still owns mutation command logic for deletion scheduling, cache clearing, entity deletion, and relation deletion alongside route definitions."
    root_cause_hypothesis: "Command-style endpoints accumulated inline in document_routes.py because they were added directly next to their routes, even though the logic depends on shared pipeline state, LightRAG command calls, and API-facing error normalization rather than route state."
    definition_of_done:
      - "Mutation command helpers live in a dedicated module"
      - "document_routes.py preserves the helper import surface through compatibility aliases"
      - "Focused tests cover re-exports plus key behaviors such as busy-state deletion refusal, cache clearing, and deletion error normalization"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/document_routes.py lightrag/api/routers/document_mutation_commands.py tests/test_document_mutation_commands.py"
      - "python -m pytest tests/test_document_mutation_commands.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/document_routes.py"
      - "lightrag/api/routers/document_mutation_commands.py"
      - "tests/test_document_mutation_commands.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving deletion scheduling, cache clearing, entity deletion, and relation deletion helpers into lightrag/api/routers/document_mutation_commands.py while keeping document_routes.py as the compatibility import surface."

  - id: S1
    title: "Extract document ingestion command helpers from document_routes.py"
    priority: 22
    status: completed
    depends_on: [R1]
    area: "api"
    problem_statement: "The document API router still owns ingestion command logic for scan startup, file upload, single-text insertion, and multi-text insertion alongside route definitions."
    root_cause_hypothesis: "Ingestion endpoints accumulated inline in document_routes.py because they combine request validation, duplicate checks, track-id generation, and background scheduling, even though that behavior depends on document-manager and pipeline helpers rather than route state."
    definition_of_done:
      - "Ingestion command helpers live in a dedicated module"
      - "document_routes.py preserves the helper import surface through compatibility aliases"
      - "Focused tests cover re-exports plus key ingestion behaviors such as scan scheduling, duplicate detection, and text enqueue startup"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/document_routes.py lightrag/api/routers/document_ingest_commands.py tests/test_document_ingest_commands.py"
      - "python -m pytest tests/test_document_ingest_commands.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/document_routes.py"
      - "lightrag/api/routers/document_ingest_commands.py"
      - "tests/test_document_ingest_commands.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving scan startup, file upload, and text insertion helpers into lightrag/api/routers/document_ingest_commands.py while keeping document_routes.py as the compatibility import surface."

  - id: T1
    title: "Extract query API models from query_routes.py"
    priority: 23
    status: completed
    depends_on: [S1]
    area: "api"
    problem_statement: "The query API router still mixes large request/response schema declarations with route implementation in the largest active API router module."
    root_cause_hypothesis: "Query contracts accumulated directly inside query_routes.py because the router became the default home for both API models and query handlers, even though the models do not depend on route state."
    definition_of_done:
      - "Query request and response models live in a dedicated module"
      - "query_routes.py preserves the existing import surface through compatibility imports"
      - "Focused tests cover model re-exports and key QueryRequest behaviors"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/query_routes.py lightrag/api/routers/query_models.py tests/test_query_models.py"
      - "python -m pytest tests/test_query_models.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/query_routes.py"
      - "lightrag/api/routers/query_models.py"
      - "tests/test_query_models.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving QueryRequest, QueryResponse, QueryDataResponse, StreamChunkResponse, and related response models into lightrag/api/routers/query_models.py while keeping query_routes.py as the compatibility import surface."

  - id: U1
    title: "Extract query response formatting helpers from query_routes.py"
    priority: 24
    status: completed
    depends_on: [T1]
    area: "api"
    problem_statement: "The query API router still duplicates reference enrichment and final response shaping logic across the /query and /query/stream handlers."
    root_cause_hypothesis: "Both query handlers evolved around the same unified `aquery_llm` result format, but their response-formatting steps stayed inline instead of converging on one helper seam."
    definition_of_done:
      - "Shared query response formatting helpers live in a dedicated module"
      - "query_routes.py preserves the helper import surface through compatibility aliases"
      - "Focused tests cover helper re-exports and key reference/response formatting behaviors"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/query_routes.py lightrag/api/routers/query_response_helpers.py tests/test_query_response_helpers.py"
      - "python -m pytest tests/test_query_response_helpers.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/query_routes.py"
      - "lightrag/api/routers/query_response_helpers.py"
      - "tests/test_query_response_helpers.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving shared reference enrichment and non-stream response payload shaping into lightrag/api/routers/query_response_helpers.py while keeping query_routes.py as the compatibility import surface."

  - id: V1
    title: "Extract query route OpenAPI response metadata from query_routes.py"
    priority: 25
    status: completed
    depends_on: [U1]
    area: "api"
    problem_statement: "The query API router still embeds three large OpenAPI response metadata blocks inline with route registration, keeping the largest active router dominated by static documentation structures instead of route behavior."
    root_cause_hypothesis: "Query route examples and response schemas evolved directly inside query_routes.py because decorator-local response metadata had no dedicated home, even though those dictionaries do not depend on FastAPI route state or runtime query execution."
    definition_of_done:
      - "Query route OpenAPI response metadata lives in a dedicated module"
      - "query_routes.py reuses the extracted response metadata constants"
      - "Focused tests cover response-doc constants and their compatibility re-export path"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/query_routes.py lightrag/api/routers/query_route_docs.py tests/test_query_route_docs.py"
      - "python -m pytest tests/test_query_route_docs.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/query_routes.py"
      - "lightrag/api/routers/query_route_docs.py"
      - "tests/test_query_route_docs.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving the /query, /query/stream, and /query/data OpenAPI response dictionaries into lightrag/api/routers/query_route_docs.py while keeping query_routes.py as the compatibility import surface."

  - id: W1
    title: "Extract query streaming helpers from query_routes.py"
    priority: 26
    status: completed
    depends_on: [V1]
    area: "api"
    problem_statement: "The query API router still owns the /query/stream NDJSON generator and StreamingResponse assembly alongside route registration."
    root_cause_hypothesis: "Streaming query behavior evolved inline in query_routes.py because the generator was nested inside the endpoint, even though NDJSON payload assembly depends on the unified query result format rather than route registration."
    definition_of_done:
      - "Streaming NDJSON payload assembly lives in a dedicated helper module"
      - "query_routes.py preserves the streaming helper import surface through compatibility aliases"
      - "Focused tests cover streaming helper re-exports, chunk filtering, error lines, and non-stream fallback payload assembly"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/query_routes.py lightrag/api/routers/query_streaming.py tests/test_query_streaming.py"
      - "python -m pytest tests/test_query_streaming.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/query_routes.py"
      - "lightrag/api/routers/query_streaming.py"
      - "tests/test_query_streaming.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving the /query/stream NDJSON iterator and StreamingResponse builder into lightrag/api/routers/query_streaming.py while keeping query_routes.py as the compatibility import surface."

  - id: X1
    title: "Extract non-stream query execution helpers from query_routes.py"
    priority: 27
    status: completed
    depends_on: [W1]
    area: "api"
    problem_statement: "The query API router still owns non-stream query execution and /query/data response normalization alongside route registration, including an invalid fallback path that omits required metadata."
    root_cause_hypothesis: "Non-stream query behavior evolved inline in query_routes.py because it looked small compared with the streaming path, so request-to-param conversion and data-response normalization never converged on a dedicated execution seam."
    definition_of_done:
      - "Non-stream /query execution and /query/data normalization live in a dedicated helper module"
      - "query_routes.py preserves the helper import surface through compatibility aliases"
      - "Focused tests cover helper re-exports, stream=False enforcement, and the invalid /query/data fallback shape"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/query_routes.py lightrag/api/routers/query_execution.py tests/test_query_execution.py"
      - "python -m pytest tests/test_query_execution.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/query_routes.py"
      - "lightrag/api/routers/query_execution.py"
      - "tests/test_query_execution.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving non-stream /query execution and /query/data response normalization into lightrag/api/routers/query_execution.py while keeping query_routes.py as the compatibility import surface and fixing the invalid fallback path to include empty metadata."

  - id: Y1
    title: "Extract query route descriptions from query_routes.py"
    priority: 28
    status: completed
    depends_on: [X1]
    area: "api"
    problem_statement: "The query API router still embeds three large long-form route descriptions inline with route registration, leaving the remaining file size dominated by static documentation text instead of route wiring."
    root_cause_hypothesis: "Query route documentation kept living in handler docstrings because FastAPI can read function docs implicitly, so the largest active router never converged on a dedicated home for its long-form description text."
    definition_of_done:
      - "Long-form query route descriptions live in a dedicated module"
      - "query_routes.py reuses the extracted description constants for route registration and re-exports them for compatibility"
      - "Focused tests cover description constants and their attachment to the registered routes"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/query_routes.py lightrag/api/routers/query_route_descriptions.py tests/test_query_route_descriptions.py"
      - "python -m pytest tests/test_query_route_descriptions.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/query_routes.py"
      - "lightrag/api/routers/query_route_descriptions.py"
      - "tests/test_query_route_descriptions.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving the /query, /query/stream, and /query/data long-form descriptions into lightrag/api/routers/query_route_descriptions.py while keeping query_routes.py as the compatibility import surface."

  - id: Z1
    title: "Make create_query_routes a true router factory"
    priority: 29
    status: completed
    depends_on: [Y1]
    area: "api"
    problem_statement: "The query route builder still registers handlers onto a shared module-level APIRouter, so repeated create_query_routes(...) calls duplicate the same /query routes."
    root_cause_hypothesis: "The module started with a singleton router for decorator convenience, and later wrapped route registration in a factory function without moving the APIRouter allocation inside that factory."
    definition_of_done:
      - "Each create_query_routes(...) call returns a fresh APIRouter instance"
      - "Repeated factory calls do not accumulate duplicate /query route registrations"
      - "Focused tests cover the fresh-router and no-duplicate-registration behavior"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/query_routes.py tests/test_query_router_factory.py"
      - "python -m pytest tests/test_query_router_factory.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/query_routes.py"
      - "tests/test_query_router_factory.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving APIRouter construction inside create_query_routes(...) so the query router builder no longer mutates a shared module-level router across repeated calls."
      - "An empty module-level router remains only as a compatibility import surface for lightrag.api.routers.__init__."

  - id: AA1
    title: "Make create_graph_routes a true router factory"
    priority: 30
    status: completed
    depends_on: [Z1]
    area: "api"
    problem_statement: "The graph route builder still registers handlers onto a shared module-level APIRouter, so repeated create_graph_routes(...) calls duplicate the same graph endpoints."
    root_cause_hypothesis: "The module started with a singleton router for decorator convenience, and later wrapped route registration in a factory function without moving the APIRouter allocation inside that factory."
    definition_of_done:
      - "Each create_graph_routes(...) call returns a fresh APIRouter instance"
      - "Repeated factory calls do not accumulate duplicate graph route registrations"
      - "Focused tests cover the fresh-router and no-duplicate-registration behavior"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/graph_routes.py tests/test_graph_router_factory.py"
      - "python -m pytest tests/test_graph_router_factory.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/graph_routes.py"
      - "tests/test_graph_router_factory.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving APIRouter construction inside create_graph_routes(...) so the graph router builder no longer mutates a shared module-level router across repeated calls."
      - "An empty module-level router remains only as a compatibility import surface for lightrag.api.routers.__init__."

  - id: AB1
    title: "Extract graph request models from graph_routes.py"
    priority: 31
    status: completed
    depends_on: [AA1]
    area: "api"
    problem_statement: "The graph API router still mixes Pydantic request model declarations with route implementation in the largest active runtime router module."
    root_cause_hypothesis: "Graph route contracts accumulated directly inside graph_routes.py because the router was the default home for both request schemas and endpoint handlers, even though the models do not depend on route state."
    definition_of_done:
      - "Graph request models live in a dedicated module"
      - "graph_routes.py preserves the existing import surface through compatibility imports"
      - "Focused tests cover model re-exports and key request-model defaults or structure"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/graph_routes.py lightrag/api/routers/graph_models.py tests/test_graph_models.py"
      - "python -m pytest tests/test_graph_models.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/graph_routes.py"
      - "lightrag/api/routers/graph_models.py"
      - "tests/test_graph_models.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving graph entity/relation create-update and merge request models into lightrag/api/routers/graph_models.py while keeping graph_routes.py as the compatibility import surface."

  - id: AC1
    title: "Extract graph route descriptions from graph_routes.py"
    priority: 32
    status: completed
    depends_on: [AB1]
    area: "api"
    problem_statement: "The graph API router still embeds several large long-form route descriptions inline with route registration, leaving the largest active graph router dominated by static documentation text instead of route wiring."
    root_cause_hypothesis: "Graph route documentation kept living in handler docstrings because FastAPI can read function docs implicitly, so graph_routes.py never converged on a dedicated home for its longest route descriptions."
    definition_of_done:
      - "Long-form graph route descriptions live in a dedicated module"
      - "graph_routes.py reuses the extracted description constants for route registration and re-exports them for compatibility"
      - "Focused tests cover description constants and their attachment to the registered routes"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/graph_routes.py lightrag/api/routers/graph_route_descriptions.py tests/test_graph_route_descriptions.py"
      - "python -m pytest tests/test_graph_route_descriptions.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/graph_routes.py"
      - "lightrag/api/routers/graph_route_descriptions.py"
      - "tests/test_graph_route_descriptions.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving the longest graph route descriptions into lightrag/api/routers/graph_route_descriptions.py while keeping graph_routes.py as the compatibility import surface."

  - id: AD1
    title: "Extract graph mutation command helpers from graph_routes.py"
    priority: 33
    status: completed
    depends_on: [AC1]
    area: "api"
    problem_statement: "The graph API router still owns write-side command logic for entity edit, relation edit, entity create, relation create, and entity merge alongside route registration."
    root_cause_hypothesis: "Graph mutation endpoints accumulated inline in graph_routes.py because they were added directly next to their routes, even though their behavior depends on LightRAG command calls and API-facing error normalization rather than route state."
    definition_of_done:
      - "Graph mutation command helpers live in a dedicated module"
      - "graph_routes.py preserves the helper import surface through compatibility aliases"
      - "Focused tests cover helper re-exports plus key behaviors such as entity-update fallback summaries, validation error mapping, and merge success messages"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/graph_routes.py lightrag/api/routers/graph_mutation_commands.py tests/test_graph_mutation_commands.py"
      - "python -m pytest tests/test_graph_mutation_commands.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/graph_routes.py"
      - "lightrag/api/routers/graph_mutation_commands.py"
      - "tests/test_graph_mutation_commands.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving graph entity/relation create-edit and entity-merge command helpers into lightrag/api/routers/graph_mutation_commands.py while keeping graph_routes.py as the compatibility import surface."

  - id: AE1
    title: "Extract read-only graph query helpers from graph_routes.py"
    priority: 34
    status: completed
    depends_on: [AD1]
    area: "api"
    problem_statement: "The graph API router still owns read-side query logic for graph labels, popular labels, label search, knowledge-graph retrieval, and entity-existence checks alongside route registration."
    root_cause_hypothesis: "Graph query endpoints accumulated inline in graph_routes.py because they looked small individually, even though together they depend on graph read calls and API-facing error normalization rather than route state."
    definition_of_done:
      - "Read-only graph query helpers live in a dedicated module"
      - "graph_routes.py preserves the helper import surface through compatibility aliases"
      - "Focused tests cover helper re-exports plus key read behaviors such as backend delegation, query-argument forwarding, error mapping, and entity-existence wrapping"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/graph_routes.py lightrag/api/routers/graph_query_helpers.py tests/test_graph_query_helpers.py"
      - "python -m pytest tests/test_graph_query_helpers.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/graph_routes.py"
      - "lightrag/api/routers/graph_query_helpers.py"
      - "tests/test_graph_query_helpers.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving graph label listing/search, knowledge-graph retrieval, and entity-existence helpers into lightrag/api/routers/graph_query_helpers.py while keeping graph_routes.py as the compatibility import surface."

  - id: AF1
    title: "Make create_document_routes a true router factory"
    priority: 35
    status: completed
    depends_on: [AE1]
    area: "api"
    problem_statement: "The document route builder still registers handlers onto a shared module-level APIRouter, so repeated create_document_routes(...) calls duplicate the same document endpoints."
    root_cause_hypothesis: "The module started with a singleton router for decorator convenience, and later wrapped route registration in a factory function without moving the APIRouter allocation inside that factory."
    definition_of_done:
      - "Each create_document_routes(...) call returns a fresh APIRouter instance"
      - "Repeated factory calls do not accumulate duplicate document route registrations"
      - "Focused tests cover the fresh-router and no-duplicate-registration behavior"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/document_routes.py tests/test_document_router_factory.py"
      - "python -m pytest tests/test_document_router_factory.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/document_routes.py"
      - "tests/test_document_router_factory.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving APIRouter construction inside create_document_routes(...) so the document router builder no longer mutates a shared module-level router across repeated calls."
      - "An empty module-level router remains only as a compatibility import surface for lightrag.api.routers.__init__."

  - id: AG1
    title: "Extract Ollama API models from ollama_api.py"
    priority: 36
    status: completed
    depends_on: [AF1]
    area: "api"
    problem_statement: "The Ollama-compatible API module still mixes request/response schemas and query-mode enum declarations with router setup and runtime stream/query behavior in the largest active API router module."
    root_cause_hypothesis: "Ollama request/response contracts accumulated directly inside ollama_api.py because the router became the default home for both API schemas and runtime handlers, even though those contracts do not depend on route state."
    definition_of_done:
      - "Ollama request/response schemas and SearchMode live in a dedicated module"
      - "ollama_api.py preserves the existing import surface through compatibility re-exports"
      - "Focused tests cover model re-exports and key parse_query_mode behaviors against the extracted enum contract"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/ollama_api.py lightrag/api/routers/ollama_models.py tests/test_ollama_models.py"
      - "python -m pytest tests/test_ollama_models.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/ollama_api.py"
      - "lightrag/api/routers/ollama_models.py"
      - "tests/test_ollama_models.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving Ollama request/response schemas and the SearchMode enum into lightrag/api/routers/ollama_models.py while keeping ollama_api.py as the compatibility import surface."

  - id: AH1
    title: "Extract Ollama request helpers from ollama_api.py"
    priority: 37
    status: completed
    depends_on: [AG1]
    area: "api"
    problem_statement: "The Ollama-compatible API module still mixes request parsing, token estimation, and query-prefix interpretation helpers with router setup and runtime stream/query behavior in the largest active API router module."
    root_cause_hypothesis: "Ollama front-door helper logic accumulated directly inside ollama_api.py because those functions were added next to the handlers that use them, even though they depend on Request/BaseModel parsing, tokenizer access, and SearchMode contracts rather than route state."
    definition_of_done:
      - "Ollama request parsing, token estimation, and query-mode helpers live in a dedicated module"
      - "ollama_api.py preserves the existing helper import surface through compatibility re-exports"
      - "Focused tests cover helper re-exports plus key request parsing and query-mode behaviors"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/ollama_api.py lightrag/api/routers/ollama_request_helpers.py tests/test_ollama_request_helpers.py"
      - "python -m pytest tests/test_ollama_request_helpers.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/ollama_api.py"
      - "lightrag/api/routers/ollama_request_helpers.py"
      - "tests/test_ollama_request_helpers.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving Ollama request parsing, token estimation, and query-prefix helpers into lightrag/api/routers/ollama_request_helpers.py while keeping ollama_api.py as the compatibility import surface."

  - id: AI1
    title: "Extract Ollama metadata response builders from ollama_api.py"
    priority: 38
    status: completed
    depends_on: [AH1]
    area: "api"
    problem_statement: "The Ollama-compatible API module still embeds static response-shaping logic for the /version, /tags, and /ps metadata endpoints alongside router setup and runtime generate/chat behavior."
    root_cause_hypothesis: "Ollama metadata payload assembly accumulated directly inside ollama_api.py because those endpoints looked small individually, even though their response-building logic depends only on server info and API contracts rather than route state."
    definition_of_done:
      - "Ollama metadata response builders live in a dedicated module"
      - "ollama_api.py preserves the existing helper import surface through compatibility re-exports"
      - "Focused tests cover helper re-exports plus key version, tag, and running-model response fields"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/ollama_api.py lightrag/api/routers/ollama_metadata_endpoints.py tests/test_ollama_metadata_endpoints.py"
      - "python -m pytest tests/test_ollama_metadata_endpoints.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/ollama_api.py"
      - "lightrag/api/routers/ollama_metadata_endpoints.py"
      - "tests/test_ollama_metadata_endpoints.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving Ollama metadata response builders for /version, /tags, and /ps into lightrag/api/routers/ollama_metadata_endpoints.py while keeping ollama_api.py as the compatibility import surface."

  - id: AJ1
    title: "Extract Ollama generate handlers from ollama_api.py"
    priority: 39
    status: completed
    depends_on: [AI1]
    area: "api"
    problem_statement: "The Ollama-compatible API module still owns the entire /generate execution path, including direct LLM invocation, non-stream fallback shaping, and NDJSON streaming assembly, alongside the separate /chat flow."
    root_cause_hypothesis: "Generate handling accumulated inline in ollama_api.py because it looked simpler than chat, even though the /generate runtime depends on rag access, token estimation, and server metadata rather than route registration."
    definition_of_done:
      - "The /generate execution path lives in a dedicated helper module"
      - "ollama_api.py preserves the generate helper import surface through compatibility re-exports"
      - "Focused tests cover helper re-exports, non-stream fallback behavior, and streaming string-response assembly"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/ollama_api.py lightrag/api/routers/ollama_generate_handlers.py tests/test_ollama_generate_handlers.py"
      - "python -m pytest tests/test_ollama_generate_handlers.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/ollama_api.py"
      - "lightrag/api/routers/ollama_generate_handlers.py"
      - "tests/test_ollama_generate_handlers.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving the /generate execution and streaming payload assembly into lightrag/api/routers/ollama_generate_handlers.py while keeping ollama_api.py as the compatibility import surface."

  - id: AK1
    title: "Extract Ollama chat handlers from ollama_api.py"
    priority: 40
    status: completed
    depends_on: [AJ1]
    area: "api"
    problem_statement: "The Ollama-compatible API module still owns the entire /chat execution path, including query-mode parsing, conversation-history adaptation, non-stream fallback shaping, and NDJSON streaming assembly, alongside route registration."
    root_cause_hypothesis: "Chat handling stayed inline in ollama_api.py because it shared helper dependencies with /generate, even though the /chat runtime depends on request parsing, QueryParam construction, and response shaping rather than router setup."
    definition_of_done:
      - "The /chat execution path lives in a dedicated helper module"
      - "ollama_api.py preserves the chat helper import surface through compatibility re-exports"
      - "Focused tests cover helper re-exports, non-stream fallback behavior, streaming string-response assembly, and stream-error normalization"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/ollama_api.py lightrag/api/routers/ollama_chat_handlers.py tests/test_ollama_chat_handlers.py"
      - "python -m pytest tests/test_ollama_chat_handlers.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/ollama_api.py"
      - "lightrag/api/routers/ollama_chat_handlers.py"
      - "tests/test_ollama_chat_handlers.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by moving the /chat execution path and streaming payload assembly into lightrag/api/routers/ollama_chat_handlers.py while keeping ollama_api.py as the compatibility import surface."

  - id: AL1
    title: "Preserve HTTPException status codes in Ollama route wrappers"
    priority: 41
    status: completed
    depends_on: [AK1]
    area: "api"
    problem_statement: "The Ollama /generate and /chat route wrappers currently catch FastAPI HTTPException instances and rewrap them as 500 responses, which turns client validation failures into server errors."
    root_cause_hypothesis: "The router-level logging wrapper uses a broad `except Exception` block around request parsing and helper execution, so framework-intended HTTP status codes never escape unchanged."
    definition_of_done:
      - "Ollama route wrappers re-raise HTTPException without converting it into a 500 response"
      - "Unexpected exceptions still log and map to 500 responses"
      - "Focused tests cover passthrough of client-facing HTTPException status codes"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/ollama_api.py tests/test_ollama_route_error_passthrough.py"
      - "python -m pytest tests/test_ollama_route_error_passthrough.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/ollama_api.py"
      - "tests/test_ollama_route_error_passthrough.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by routing generate/chat wrapper failures through a shared helper that re-raises HTTPException unchanged and only maps unexpected exceptions to 500 responses."
      - "Focused regression coverage lives in tests/test_ollama_route_error_passthrough.py."

  - id: AM1
    title: "Extract shared Ollama streaming-response wrapper"
    priority: 42
    status: completed
    depends_on: [AL1]
    area: "api"
    problem_statement: "The extracted Ollama generate and chat handler modules both build the same NDJSON StreamingResponse wrapper inline, duplicating headers and response-shaping setup."
    root_cause_hypothesis: "Streaming response assembly stayed attached to each endpoint-specific handler because the earlier refactors focused on splitting runtime flows first, so the cross-endpoint NDJSON transport seam never got its own shared helper."
    definition_of_done:
      - "A shared helper builds the standard Ollama NDJSON StreamingResponse wrapper"
      - "Generate and chat handlers reuse that helper instead of duplicating the header/media-type setup"
      - "Focused tests cover the shared wrapper contract or the preserved header behavior at the touched seam"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/ollama_generate_handlers.py lightrag/api/routers/ollama_chat_handlers.py lightrag/api/routers/ollama_streaming_responses.py tests/test_ollama_streaming_responses.py"
      - "python -m pytest tests/test_ollama_streaming_responses.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/ollama_generate_handlers.py"
      - "lightrag/api/routers/ollama_chat_handlers.py"
      - "lightrag/api/routers/ollama_streaming_responses.py"
      - "tests/test_ollama_streaming_responses.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by extracting the standard NDJSON StreamingResponse wrapper into lightrag/api/routers/ollama_streaming_responses.py and reusing it from both generate and chat handlers."
      - "Focused regression coverage lives in tests/test_ollama_streaming_responses.py."

  - id: AN1
    title: "Extract shared Ollama stream completion payload builders"
    priority: 43
    status: completed
    depends_on: [AM1]
    area: "api"
    problem_statement: "The Ollama generate and chat stream iterators still duplicate the final done payload timing/accounting block and their provider-error terminal payload shaping."
    root_cause_hypothesis: "The earlier refactors split route/runtime ownership first and then centralized the transport wrapper, but the endpoint iterators still carry repeated completion and error payload construction because the payload shapes look slightly different at first glance."
    definition_of_done:
      - "Shared helpers build the common Ollama stream completion and error payload structure"
      - "Generate and chat iterators reuse those helpers instead of duplicating the timing/accounting boilerplate"
      - "Focused tests cover the shared payload-builder contract at the touched seam"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/ollama_generate_handlers.py lightrag/api/routers/ollama_chat_handlers.py lightrag/api/routers/ollama_stream_payloads.py tests/test_ollama_stream_payloads.py"
      - "python -m pytest tests/test_ollama_stream_payloads.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/ollama_generate_handlers.py"
      - "lightrag/api/routers/ollama_chat_handlers.py"
      - "lightrag/api/routers/ollama_stream_payloads.py"
      - "tests/test_ollama_stream_payloads.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Completed by extracting shared done/error payload builders plus shared stream-error normalization into lightrag/api/routers/ollama_stream_payloads.py and reusing them from both generate and chat iterators."
      - "Focused regression coverage lives in tests/test_ollama_stream_payloads.py."

  - id: AO1
    title: "Extract shared Ollama stream chunk payload builders"
    priority: 44
    status: completed
    depends_on: [AN1]
    area: "api"
    problem_statement: "The Ollama generate and chat stream iterators still duplicate the per-chunk payload shaping for normal streaming tokens and for the initial string-response chunk path."
    root_cause_hypothesis: "After transport and terminal payload builders were centralized, the remaining duplication is the non-terminal chunk payload structure, which stayed inline because generate uses `response` while chat uses nested `message.content`."
    definition_of_done:
      - "Shared helpers build the common Ollama non-terminal stream chunk payload structure"
      - "Generate and chat iterators reuse those helpers instead of duplicating chunk payload assembly"
      - "Focused tests cover the shared chunk-payload contract at the touched seam"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/ollama_generate_handlers.py lightrag/api/routers/ollama_chat_handlers.py lightrag/api/routers/ollama_stream_payloads.py tests/test_ollama_stream_payloads.py"
      - "python -m pytest tests/test_ollama_stream_payloads.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/ollama_generate_handlers.py"
      - "lightrag/api/routers/ollama_chat_handlers.py"
      - "lightrag/api/routers/ollama_stream_payloads.py"
      - "tests/test_ollama_stream_payloads.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Queued from AN1 follow-up analysis after the terminal payload seam was centralized but both iterators still duplicated their non-terminal chunk payload assembly."
      - "Completed by introducing shared non-terminal chunk payload builders in lightrag/api/routers/ollama_stream_payloads.py and reusing them from both the string-response and async-stream paths in the generate/chat iterators."
      - "Focused regression coverage lives in tests/test_ollama_stream_payloads.py."

  - id: AP1
    title: "Extract an explicit Ollama router factory from ollama_api.py"
    priority: 45
    status: completed
    depends_on: [AO1]
    area: "api"
    problem_statement: "The Ollama-compatible router module still hides route registration inside the stateful `OllamaAPI.setup_routes()` method even though the actual request/runtime logic has already been extracted into dedicated helper modules."
    root_cause_hypothesis: "Earlier Ollama refactors split runtime helpers first, but the module kept its original class wrapper, so route composition still depends on instance state and nested closures instead of an explicit router factory seam."
    definition_of_done:
      - "A dedicated helper returns a fresh Ollama APIRouter from explicit inputs"
      - "`route_registry.py` uses the explicit Ollama router factory instead of depending on an `OllamaAPI(...).router` side effect"
      - "The `OllamaAPI` compatibility surface remains available for existing imports"
      - "Focused tests cover fresh-router behavior plus the preserved compatibility wrapper"
    validation_commands:
      - "python -m ruff check lightrag/api/routers/ollama_api.py lightrag/api/route_registry.py tests/test_ollama_api_router_factory.py tests/test_route_registry.py tests/test_ollama_route_error_passthrough.py"
      - "python -m pytest tests/test_ollama_api_router_factory.py tests/test_route_registry.py tests/test_ollama_route_error_passthrough.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/routers/ollama_api.py"
      - "lightrag/api/route_registry.py"
      - "tests/test_ollama_api_router_factory.py"
      - "tests/test_route_registry.py"
      - "tests/test_ollama_route_error_passthrough.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Queued after AO1 root-cause analysis showed the remaining structural issue in ollama_api.py is route composition still living inside a stateful wrapper class instead of an explicit APIRouter factory."
      - "Completed by introducing create_ollama_router(...) as the explicit APIRouter factory, rewiring route_registry.py to consume it directly, and keeping OllamaAPI as a thin compatibility wrapper that still exposes `.router`."
      - "Focused regression coverage lives in tests/test_ollama_api_router_factory.py, with route_registry and route-error tests updated to preserve the existing compatibility surface."

  - id: AQ1
    title: "Extract auth status and login routes from route_registry.py"
    priority: 46
    status: completed
    depends_on: [AP1]
    area: "api"
    problem_statement: "The app route registry still defines the root `/auth-status` and `/login` endpoints inline, so app composition owns auth response shaping and duplicates the guest-mode response payload structure."
    root_cause_hypothesis: "D1 separated router-family inclusion from app startup, but the root auth endpoints remained in `route_registry.py` because they depend on the resolved auth handler and version payload, so the auth route seam never became its own explicit router factory."
    definition_of_done:
      - "A dedicated router factory owns the `/auth-status` and `/login` route logic"
      - "`route_registry.py` includes that auth router instead of defining the auth endpoints inline"
      - "Guest-mode payload shaping is centralized instead of duplicated across the two auth endpoints"
      - "Focused tests cover the auth router contract plus preserved route-registry behavior"
    validation_commands:
      - "python -m ruff check lightrag/api/app_auth_routes.py lightrag/api/route_registry.py tests/test_app_auth_routes.py tests/test_route_registry.py"
      - "python -m pytest tests/test_app_auth_routes.py tests/test_route_registry.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/app_auth_routes.py"
      - "lightrag/api/route_registry.py"
      - "tests/test_app_auth_routes.py"
      - "tests/test_route_registry.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Queued after AP1 root-cause analysis showed the next narrow route-registry seam is the inline auth-status/login pair and their duplicated guest-mode payload shaping."
      - "Completed by introducing create_auth_router(...) in lightrag/api/app_auth_routes.py, rewiring route_registry.py to include that router, and centralizing the shared guest-mode response payload builder."
      - "Focused regression coverage lives in tests/test_app_auth_routes.py, with tests/test_route_registry.py preserving the existing app-level behavior."

  - id: AR1
    title: "Extract the health route from route_registry.py"
    priority: 47
    status: completed
    depends_on: [AQ1]
    area: "api"
    problem_statement: "The app route registry still defines the root `/health` endpoint inline, so app composition owns workspace resolution, health payload assembly, and error mapping for the system-status surface."
    root_cause_hypothesis: "D1 split router-family inclusion from app startup, but the root health endpoint remained in `route_registry.py` because it depends on runtime args, workspace helpers, and auth state, so the health route seam never became its own explicit router factory."
    definition_of_done:
      - "A dedicated router factory owns the `/health` route logic"
      - "`route_registry.py` includes that health router instead of defining the route inline"
      - "Workspace resolution and health payload shaping live behind the extracted health-route seam"
      - "Focused tests cover the health router contract plus preserved route-registry behavior"
    validation_commands:
      - "python -m ruff check lightrag/api/app_health_routes.py lightrag/api/route_registry.py tests/test_app_health_routes.py tests/test_route_registry.py"
      - "python -m pytest tests/test_app_health_routes.py tests/test_route_registry.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/app_health_routes.py"
      - "lightrag/api/route_registry.py"
      - "tests/test_app_health_routes.py"
      - "tests/test_route_registry.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Queued after AQ1 root-cause analysis showed the next narrow route-registry seam is the inline health endpoint and its mixed responsibility for workspace lookup, status reads, and response shaping."
      - "Completed by introducing create_health_router(...) in lightrag/api/app_health_routes.py, rewiring route_registry.py to include that router, and centralizing workspace resolution plus health payload assembly behind the extracted seam."
      - "Focused regression coverage lives in tests/test_app_health_routes.py, with tests/test_route_registry.py preserving the app-composition behavior."

  - id: AS1
    title: "Extract the app shell routes from route_registry.py"
    priority: 48
    status: completed
    depends_on: [AR1]
    area: "api"
    problem_statement: "The app route registry still defines the app shell routes inline, so app composition owns Swagger UI route wiring, root redirect behavior, and the `/webui` fallback redirect when WebUI assets are missing."
    root_cause_hypothesis: "D1 split router-family inclusion from app startup, but the app shell routes stayed in `route_registry.py` because they depend on FastAPI app metadata and `webui_assets_exist`, so that shell-routing seam never became its own explicit router factory."
    definition_of_done:
      - "A dedicated router factory owns `/docs`, `/docs/oauth2-redirect`, `/`, and the no-WebUI `/webui` fallback redirect behavior"
      - "`route_registry.py` includes that app shell router instead of defining those routes inline"
      - "The extracted shell router preserves the existing Swagger UI and redirect behavior"
      - "Focused tests cover the shell-router contract plus preserved route-registry behavior"
    validation_commands:
      - "python -m ruff check lightrag/api/app_shell_routes.py lightrag/api/route_registry.py tests/test_app_shell_routes.py tests/test_route_registry.py"
      - "python -m pytest tests/test_app_shell_routes.py tests/test_route_registry.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/app_shell_routes.py"
      - "lightrag/api/route_registry.py"
      - "tests/test_app_shell_routes.py"
      - "tests/test_route_registry.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Queued after AR1 root-cause analysis showed the next narrow route-registry seam is the inline app shell route set for Swagger docs, root redirect, and no-WebUI fallback."
      - "Completed by introducing create_app_shell_router(...) in lightrag/api/app_shell_routes.py, rewiring route_registry.py to include that router, and moving the root/docs/no-WebUI redirect behavior behind the extracted shell seam."
      - "Focused regression coverage lives in tests/test_app_shell_routes.py, with tests/test_route_registry.py preserving the existing app-composition behavior."

  - id: AT1
    title: "Extract static asset mounting from route_registry.py"
    priority: 49
    status: completed
    depends_on: [AS1]
    area: "api"
    problem_statement: "The app route registry still owns the Swagger static mount, the WebUI static mount, and the `SmartStaticFiles` cache-header policy, so app composition continues to mix static asset delivery with router assembly."
    root_cause_hypothesis: "D1 and the follow-on route extractions focused on HTTP route handlers first, but the static mount block remained in `route_registry.py` because it depends on local asset paths and the custom WebUI cache policy, so the static-delivery seam never became its own helper module."
    definition_of_done:
      - "A dedicated helper module owns Swagger/WebUI static mounting and the custom WebUI static-files response policy"
      - "`route_registry.py` uses that helper instead of mounting static assets inline"
      - "Focused tests cover both the extracted mount behavior and the preserved cache-header contract"
    validation_commands:
      - "python -m ruff check lightrag/api/app_static_mounts.py lightrag/api/route_registry.py tests/test_app_static_mounts.py tests/test_route_registry.py"
      - "python -m pytest tests/test_app_static_mounts.py tests/test_route_registry.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/app_static_mounts.py"
      - "lightrag/api/route_registry.py"
      - "tests/test_app_static_mounts.py"
      - "tests/test_route_registry.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Queued after AS1 root-cause analysis showed the next narrow route-registry seam is the inline static asset mount block plus the custom WebUI cache-header policy."
      - "Completed by introducing register_static_mounts(...) and SmartStaticFiles in lightrag/api/app_static_mounts.py, rewiring route_registry.py to delegate static mounting there, and adding focused mount/header tests."
      - "The new cache-header coverage also fixed a real Windows path-normalization bug where `assets\\\\...` paths skipped the intended long-lived WebUI asset cache policy."

  - id: AU1
    title: "Extract route-registry support context from route_registry.py"
    priority: 50
    status: completed
    depends_on: [AT1]
    area: "api"
    problem_statement: "The route registry still owns `RouteRegistryContext` plus the auth-handler and version-payload helper contracts, so app composition continues to mix router assembly with support-context derivation."
    root_cause_hypothesis: "The route extraction series focused on moving runtime route logic out of `route_registry.py`, but the support contract for app assembly stayed inline because it was small and only had one immediate caller, so the composition module never became a pure registry."
    definition_of_done:
      - "A dedicated module owns `RouteRegistryContext`, version payload shaping, and auth-handler resolution"
      - "`route_registry.py` and `lightrag_server.py` use the extracted support-context module"
      - "The `route_registry.py` compatibility surface for `RouteRegistryContext` remains available"
      - "Focused tests cover the extracted helper contract plus preserved compatibility import behavior"
    validation_commands:
      - "python -m ruff check lightrag/api/app_route_context.py lightrag/api/route_registry.py lightrag/api/lightrag_server.py tests/test_app_route_context.py tests/test_route_registry.py"
      - "python -m pytest tests/test_app_route_context.py tests/test_route_registry.py -q"
    risk_level: "low"
    expected_files:
      - "lightrag/api/app_route_context.py"
      - "lightrag/api/route_registry.py"
      - "lightrag/api/lightrag_server.py"
      - "tests/test_app_route_context.py"
      - "tests/test_route_registry.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Queued after AT1 root-cause analysis showed the remaining route-registry responsibility is the support contract for assembly context and derived auth/version helpers, not route logic."
      - "Completed by introducing lightrag/api/app_route_context.py for RouteRegistryContext plus the version/auth helper contract, rewiring route_registry.py and lightrag_server.py to use it, and preserving the route_registry compatibility export."
      - "Focused regression coverage lives in tests/test_app_route_context.py, with tests/test_route_registry.py continuing to exercise the registry assembly seam."

  - id: AV1
    title: "Extract the frontend build checker from lightrag_server.py"
    priority: 51
    status: completed
    depends_on: [AU1]
    area: "api"
    problem_statement: "The FastAPI server entrypoint still owns the WebUI build existence/freshness probe, so app startup mixes frontend asset diagnostics with server construction."
    root_cause_hypothesis: "Earlier API cleanup focused on route composition and support contracts first, but `check_frontend_build()` stayed inside `lightrag_server.py` because it was a startup-only helper, so the frontend diagnostic seam never became its own module."
    definition_of_done:
      - "A dedicated helper module owns the frontend build existence and freshness checks"
      - "`lightrag_server.py` uses that helper instead of defining the checker inline"
      - "The existing `check_frontend_build` compatibility surface remains available from `lightrag_server.py`"
      - "Focused tests cover missing-build, up-to-date, and outdated-source behaviors at the extracted seam"
    validation_commands:
      - "python -m ruff check lightrag/api/frontend_build_checker.py lightrag/api/lightrag_server.py tests/test_frontend_build_checker.py"
      - "python -m pytest tests/test_frontend_build_checker.py -q"
    risk_level: "low"
    expected_files:
      - "lightrag/api/frontend_build_checker.py"
      - "lightrag/api/lightrag_server.py"
      - "tests/test_frontend_build_checker.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Queued after AU1 root-cause analysis showed the next narrow self-contained seam in lightrag_server.py is the frontend build/freshness probe rather than the larger model-binding setup."
      - "Completed by introducing lightrag/api/frontend_build_checker.py for the WebUI build existence/freshness probe, rewiring lightrag_server.py to import it, and preserving the `check_frontend_build` compatibility surface from the server module."
      - "Focused regression coverage lives in tests/test_frontend_build_checker.py."

  - id: AW1
    title: "Extract the /query/data validation error handler from lightrag_server.py"
    priority: 52
    status: completed
    depends_on: [AV1]
    area: "api"
    problem_statement: "The FastAPI server entrypoint still defines the custom RequestValidationError handler for `/query/data`, so app startup owns endpoint-specific validation formatting."
    root_cause_hypothesis: "Route and startup extractions focused on bigger seams first, but the `/query/data` validation override stayed inside `create_app()` because it was a single closure over FastAPI, so that endpoint-specific error-formatting contract never became its own helper."
    definition_of_done:
      - "A dedicated helper module owns the `/query/data` validation error formatting contract"
      - "`lightrag_server.py` registers that helper instead of defining the validation handler inline"
      - "The existing validation behavior for `/query/data` and all other endpoints remains unchanged"
      - "Focused tests cover both the data-endpoint override and the default 422 fallback behavior"
    validation_commands:
      - "python -m ruff check lightrag/api/query_validation_handlers.py lightrag/api/lightrag_server.py tests/test_query_validation_handlers.py"
      - "python -m pytest tests/test_query_validation_handlers.py -q"
    risk_level: "low"
    expected_files:
      - "lightrag/api/query_validation_handlers.py"
      - "lightrag/api/lightrag_server.py"
      - "tests/test_query_validation_handlers.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Queued after AV1 root-cause analysis showed the next narrow self-contained seam in create_app() is the endpoint-specific validation handler for `/query/data`."
      - "Completed by introducing lightrag/api/query_validation_handlers.py for the `/query/data` validation formatting contract and rewiring lightrag_server.py to register that helper instead of defining the handler inline."
      - "Focused regression coverage lives in tests/test_query_validation_handlers.py."

  - id: AX1
    title: "Extract CORS config parsing from lightrag_server.py"
    priority: 53
    status: completed
    depends_on: [AW1]
    area: "api"
    problem_statement: "The FastAPI server entrypoint still parses `global_args.cors_origins` and registers the CORS middleware inline, so app startup owns middleware-specific origin parsing."
    root_cause_hypothesis: "Startup helper extraction focused on more visible seams first, but the CORS parsing closure stayed inside `create_app()` because it was small and only used once, so the CORS configuration contract never became its own helper module."
    definition_of_done:
      - "A dedicated helper module owns CORS origin parsing and middleware registration"
      - "`lightrag_server.py` uses that helper instead of defining the CORS parsing closure inline"
      - "The existing wildcard and comma-separated origin behaviors remain unchanged"
      - "Focused tests cover both origin parsing and middleware registration at the extracted seam"
    validation_commands:
      - "python -m ruff check lightrag/api/app_cors.py lightrag/api/lightrag_server.py tests/test_app_cors.py"
      - "python -m pytest tests/test_app_cors.py -q"
    risk_level: "low"
    expected_files:
      - "lightrag/api/app_cors.py"
      - "lightrag/api/lightrag_server.py"
      - "tests/test_app_cors.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Queued after AW1 root-cause analysis showed the next narrow self-contained seam in create_app() is the CORS origin parsing and middleware registration block."
      - "Completed by introducing lightrag/api/app_cors.py for origin parsing plus middleware registration, rewiring lightrag_server.py to delegate to that helper, and preserving the existing wildcard and comma-separated origin behaviors."
      - "Focused regression coverage lives in tests/test_app_cors.py."

  - id: AY1
    title: "Extract the app lifespan from lightrag_server.py"
    priority: 54
    status: completed
    depends_on: [AX1]
    area: "api"
    problem_statement: "The FastAPI server entrypoint still defines the application lifespan inline, so app startup owns storage initialization, migration, shutdown cleanup, and Gunicorn/Uvicorn lifecycle branching in one closure."
    root_cause_hypothesis: "Previous extractions focused on startup helpers that were independent of runtime state, but the lifespan block stayed inside `create_app()` because it closes over `rag`, logging, and shared-storage cleanup, so the lifecycle contract never became its own helper module."
    definition_of_done:
      - "A dedicated helper module owns the FastAPI lifespan contract for storage init, migration, ready logging, and shutdown cleanup"
      - "`lightrag_server.py` uses that helper instead of defining the lifespan inline"
      - "The existing Gunicorn/Uvicorn cleanup branch remains unchanged"
      - "Focused tests cover startup, shutdown, and the shared-storage cleanup branch behavior"
    validation_commands:
      - "python -m ruff check lightrag/api/app_lifespan.py lightrag/api/lightrag_server.py tests/test_app_lifespan.py"
      - "python -m pytest tests/test_app_lifespan.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/app_lifespan.py"
      - "lightrag/api/lightrag_server.py"
      - "tests/test_app_lifespan.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Queued after AX1 root-cause analysis showed the next narrow self-contained seam in create_app() is the inline FastAPI lifespan block rather than the larger model-binding helpers."
      - "Completed by introducing lightrag/api/app_lifespan.py for startup/shutdown orchestration, rewiring lightrag_server.py to instantiate FastAPI with that helper after `rag` is constructed, and removing the inline lifecycle closure."
      - "Focused regression coverage lives in tests/test_app_lifespan.py."

  - id: AZ1
    title: "Extract FastAPI app kwargs assembly from lightrag_server.py"
    priority: 55
    status: completed
    depends_on: [AY1]
    area: "api"
    problem_statement: "The FastAPI server entrypoint still assembles the app title, Swagger description, OpenAPI/ReDoc URLs, and Swagger UI parameters inline, so app construction owns static API metadata and docs UI policy alongside runtime wiring."
    root_cause_hypothesis: "Previous extractions focused on runtime-heavy startup seams first, but the FastAPI kwargs block stayed inside `create_app()` because it only depended on `api_key` and the API version string, so the static app-configuration contract never became its own helper module."
    definition_of_done:
      - "A dedicated helper module owns the FastAPI app kwargs assembly for title, description, docs URLs, and Swagger UI parameters"
      - "`lightrag_server.py` uses that helper instead of composing the kwargs inline"
      - "The existing API-key-enabled description note and Swagger UI parameter behavior remain unchanged"
      - "Focused tests cover the extracted app-kwargs contract with and without an API key"
    validation_commands:
      - "python -m ruff check lightrag/api/app_factory_config.py lightrag/api/lightrag_server.py tests/test_app_factory_config.py"
      - "python -m pytest tests/test_app_factory_config.py -q"
    risk_level: "low"
    expected_files:
      - "lightrag/api/app_factory_config.py"
      - "lightrag/api/lightrag_server.py"
      - "tests/test_app_factory_config.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Queued after AY1 root-cause analysis showed the next narrow self-contained seam in create_app() is the static FastAPI kwargs block for app metadata and Swagger UI policy."
      - "Completed by introducing lightrag/api/app_factory_config.py for static FastAPI metadata and Swagger UI kwargs, then rewiring lightrag_server.py to consume that helper instead of composing the kwargs inline."
      - "Focused regression coverage lives in tests/test_app_factory_config.py."

  - id: BA1
    title: "Extract app runtime arg normalization from lightrag_server.py"
    priority: 56
    status: completed
    depends_on: [AZ1]
    area: "api"
    problem_statement: "The FastAPI server entrypoint still validates supported bindings, fills missing binding hosts, and enforces SSL file requirements inline, so app startup owns a runtime-argument normalization contract alongside app assembly."
    root_cause_hypothesis: "Previous extractions peeled off startup helpers around app construction first, but the runtime arg checks stayed inside `create_app()` because they only depended on `args` and `get_default_host()`, so the server's preflight argument contract never became its own helper seam."
    definition_of_done:
      - "A dedicated helper module owns runtime arg validation for supported bindings, default binding hosts, and SSL file requirements"
      - "`lightrag_server.py` uses that helper instead of normalizing the runtime args inline"
      - "The existing unsupported-binding errors, default-host behavior, and SSL validation messages remain unchanged"
      - "Focused tests cover supported-host filling plus the validation error branches"
    validation_commands:
      - "python -m ruff check lightrag/api/app_runtime_args.py lightrag/api/lightrag_server.py tests/test_app_runtime_args.py"
      - "python -m pytest tests/test_app_runtime_args.py -q"
    risk_level: "low"
    expected_files:
      - "lightrag/api/app_runtime_args.py"
      - "lightrag/api/lightrag_server.py"
      - "tests/test_app_runtime_args.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Queued after AZ1 root-cause analysis showed the next narrow self-contained seam in create_app() is the runtime argument preflight block for binding support, default hosts, and SSL file validation."
      - "Completed by introducing lightrag/api/app_runtime_args.py for supported-binding checks, default-host filling, and SSL file validation, then rewiring lightrag_server.py to delegate runtime arg normalization through that helper."
      - "Focused regression coverage lives in tests/test_app_runtime_args.py."

  - id: BB1
    title: "Extract LLMConfigCache from lightrag_server.py"
    priority: 57
    status: completed
    depends_on: [BA1]
    area: "api"
    problem_statement: "The FastAPI server entrypoint still defines `LLMConfigCache` inline, so app startup owns binding-option preprocessing and provider-specific logging alongside application assembly."
    root_cause_hypothesis: "Startup helper extraction has focused on `create_app()` blocks first, but the `LLMConfigCache` class stayed in `lightrag_server.py` because it only had one caller there, so the binding-option cache contract never became its own module."
    definition_of_done:
      - "A dedicated helper module owns `LLMConfigCache`"
      - "`lightrag_server.py` imports that helper instead of defining the cache class inline"
      - "The existing selective binding-option loading behavior remains unchanged"
      - "Focused tests cover the extracted cache contract for representative LLM and embedding bindings"
    validation_commands:
      - "python -m ruff check lightrag/api/llm_config_cache.py lightrag/api/lightrag_server.py tests/test_llm_config_cache.py"
      - "python -m pytest tests/test_llm_config_cache.py -q"
    risk_level: "low"
    expected_files:
      - "lightrag/api/llm_config_cache.py"
      - "lightrag/api/lightrag_server.py"
      - "tests/test_llm_config_cache.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Queued after BA1 root-cause analysis showed the next narrow self-contained seam in `lightrag_server.py` is the binding-option preprocessing cache class rather than another tiny inline value assignment."
      - "Completed by introducing lightrag/api/llm_config_cache.py as the authoritative home for `LLMConfigCache`, then rewiring lightrag_server.py to import that helper instead of defining the cache class inline."
      - "Focused regression coverage lives in tests/test_llm_config_cache.py."

  - id: BC1
    title: "Extract app startup state from lightrag_server.py"
    priority: 58
    status: completed
    depends_on: [BB1]
    area: "api"
    problem_statement: "The FastAPI server entrypoint still derives startup-only app state inline, including frontend build status, API version display, API key resolution, and document-manager construction, so app assembly owns a mixed bundle of startup context plumbing."
    root_cause_hypothesis: "Previous extractions targeted self-contained helper blocks first, but the startup-state values stayed in `create_app()` because they were short and shared only inside the route-registry context assembly, so the startup context contract never became its own helper seam."
    definition_of_done:
      - "A dedicated helper module owns startup-state derivation for frontend build status, API version display, API key resolution, and document-manager creation"
      - "`lightrag_server.py` uses that helper instead of deriving those startup values inline"
      - "The existing frontend-outdated version marker, API-key precedence, and document-manager workspace behavior remain unchanged"
      - "Focused tests cover the extracted startup-state contract"
    validation_commands:
      - "python -m ruff check lightrag/api/app_startup_state.py lightrag/api/lightrag_server.py tests/test_app_startup_state.py"
      - "python -m pytest tests/test_app_startup_state.py -q"
    risk_level: "low"
    expected_files:
      - "lightrag/api/app_startup_state.py"
      - "lightrag/api/lightrag_server.py"
      - "tests/test_app_startup_state.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Queued after BB1 root-cause analysis showed the next narrow self-contained seam in `create_app()` is the startup-state derivation block that feeds route-registry context and app metadata."
      - "Completed by introducing lightrag/api/app_startup_state.py for frontend build status, API version display, API key resolution, and document-manager creation, then rewiring lightrag_server.py to consume that startup-state helper."
      - "Focused regression coverage lives in tests/test_app_startup_state.py."

  - id: BD1
    title: "Extract LLM model kwargs builder from lightrag_server.py"
    priority: 59
    status: completed
    depends_on: [BC1]
    area: "api"
    problem_statement: "The FastAPI server entrypoint still builds binding-specific `llm_model_kwargs` inline, so app construction owns provider-specific kwargs assembly for Ollama-compatible bindings."
    root_cause_hypothesis: "Previous extractions focused on larger startup seams first, but `create_llm_model_kwargs()` stayed inside `create_app()` because it was small and only had one caller, so the binding-specific kwargs contract never became its own helper module."
    definition_of_done:
      - "A dedicated helper module owns LLM model kwargs assembly for supported bindings"
      - "`lightrag_server.py` uses that helper instead of defining `create_llm_model_kwargs()` inline"
      - "The existing Ollama-compatible host, timeout, options, and API-key behavior remain unchanged"
      - "Focused tests cover the extracted kwargs builder for both supported and fallback bindings"
    validation_commands:
      - "python -m ruff check lightrag/api/llm_model_kwargs.py lightrag/api/lightrag_server.py tests/test_llm_model_kwargs.py"
      - "python -m pytest tests/test_llm_model_kwargs.py -q"
    risk_level: "low"
    expected_files:
      - "lightrag/api/llm_model_kwargs.py"
      - "lightrag/api/lightrag_server.py"
      - "tests/test_llm_model_kwargs.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Queued after BC1 root-cause analysis showed the next narrow self-contained seam in `create_app()` is the binding-specific `llm_model_kwargs` builder rather than the larger LLM function factory block."
      - "Completed by introducing lightrag/api/llm_model_kwargs.py for binding-specific `llm_model_kwargs` assembly, then rewiring lightrag_server.py to call that helper instead of defining the builder inline."
      - "Focused regression coverage lives in tests/test_llm_model_kwargs.py."

  - id: BE1
    title: "Extract the LLM model function factory from lightrag_server.py"
    priority: 60
    status: completed
    depends_on: [BD1]
    area: "api"
    problem_statement: "The FastAPI server entrypoint still defines provider-specific LLM wrapper builders plus `create_llm_model_func()` inline, so app construction owns LLM dispatch and wrapper policy alongside runtime wiring."
    root_cause_hypothesis: "Previous extractions removed several startup seams first, but the LLM function factory stayed inside `create_app()` because it closes over `config_cache`, `args`, `llm_timeout`, and the Bedrock callable, so the provider-specific LLM dispatch contract never became its own helper module."
    definition_of_done:
      - "A dedicated helper module owns provider-specific LLM function factory dispatch"
      - "`lightrag_server.py` uses that helper instead of defining the LLM function factory inline"
      - "The existing OpenAI-compatible timeout/config merge, Gemini generation-config behavior, and Bedrock pass-through remain unchanged"
      - "Focused tests cover representative LLM wrapper and dispatch behavior at the extracted seam"
    validation_commands:
      - "python -m ruff check lightrag/api/llm_model_factory.py lightrag/api/lightrag_server.py tests/test_llm_model_factory.py"
      - "python -m pytest tests/test_llm_model_factory.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/llm_model_factory.py"
      - "lightrag/api/lightrag_server.py"
      - "tests/test_llm_model_factory.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Queued after BD1 root-cause analysis showed the next narrow self-contained seam in `create_app()` is the provider-specific LLM function factory block rather than the larger embedding or rerank setup."
      - "Completed by introducing lightrag/api/llm_model_factory.py for provider-specific LLM wrapper builders plus dispatch, then rewiring lightrag_server.py to consume that helper instead of defining the factory inline."
      - "Focused regression coverage lives in tests/test_llm_model_factory.py."

  - id: BF1
    title: "Absorb the Bedrock LLM callable into llm_model_factory.py"
    priority: 61
    status: completed
    depends_on: [BE1]
    area: "api"
    problem_statement: "Even after `BE1`, the FastAPI server entrypoint still defines the Bedrock LLM callable inline and passes it into `llm_model_factory`, so app construction still owns the last provider-specific LLM wrapper behavior."
    root_cause_hypothesis: "The initial LLM-factory extraction kept Bedrock local because it already existed as a closure over `args`, but that left `lightrag_server.py` with one remaining provider-specific LLM branch and kept the factory helper from becoming fully authoritative for LLM dispatch."
    definition_of_done:
      - "`llm_model_factory.py` owns the Bedrock LLM callable behavior along with the other provider-specific wrappers"
      - "`lightrag_server.py` no longer defines or passes a Bedrock LLM callable into the factory"
      - "The existing Bedrock temperature override and history defaulting behavior remain unchanged"
      - "Focused tests cover the extracted Bedrock callable behavior through the factory seam"
    validation_commands:
      - "python -m ruff check lightrag/api/llm_model_factory.py lightrag/api/lightrag_server.py tests/test_llm_model_factory.py"
      - "python -m pytest tests/test_llm_model_factory.py -q"
    risk_level: "low"
    expected_files:
      - "lightrag/api/llm_model_factory.py"
      - "lightrag/api/lightrag_server.py"
      - "tests/test_llm_model_factory.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Queued after BE1 root-cause analysis showed the remaining provider-specific LLM seam in `lightrag_server.py` is the local Bedrock callable rather than the larger embedding or rerank setup."
      - "Completed by moving the Bedrock LLM callable behavior into lightrag/api/llm_model_factory.py and rewiring lightrag_server.py to stop defining or passing a local Bedrock wrapper."
      - "Focused regression coverage lives in tests/test_llm_model_factory.py."

  - id: BG1
    title: "Extract rerank model setup from lightrag_server.py"
    priority: 62
    status: completed
    depends_on: [BF1]
    area: "api"
    problem_statement: "The FastAPI server entrypoint still configures rerank provider selection, default rerank args, and the rerank wrapper inline, so app construction owns provider-specific rerank wiring alongside runtime assembly."
    root_cause_hypothesis: "Previous extractions targeted startup, LLM, and config seams first, but the rerank block stayed inside `create_app()` because it only had one caller and mutates `args` for default model/base_url resolution, so the rerank setup contract never became its own helper module."
    definition_of_done:
      - "A dedicated helper module owns rerank provider selection, default arg filling, and rerank wrapper construction"
      - "`lightrag_server.py` uses that helper instead of defining the rerank setup inline"
      - "The existing unsupported-binding error, default model/base_url resolution, and `extra_body` forwarding behavior remain unchanged"
      - "Focused tests cover disabled, default-resolution, wrapper forwarding, and unsupported-binding behavior"
    validation_commands:
      - "python -m ruff check lightrag/api/rerank_model_factory.py lightrag/api/lightrag_server.py tests/test_rerank_model_factory.py"
      - "python -m pytest tests/test_rerank_model_factory.py -q"
    risk_level: "low"
    expected_files:
      - "lightrag/api/rerank_model_factory.py"
      - "lightrag/api/lightrag_server.py"
      - "tests/test_rerank_model_factory.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Queued after BF1 root-cause analysis showed the next narrow self-contained seam in `create_app()` is the provider-specific rerank setup block rather than the larger embedding function factory."
      - "Completed by introducing lightrag/api/rerank_model_factory.py for rerank provider selection, default arg filling, and wrapper construction, then rewiring lightrag_server.py to consume that helper instead of defining rerank setup inline."
      - "Focused regression coverage lives in tests/test_rerank_model_factory.py."

  - id: BH1
    title: "Extract embedding send-dim policy from lightrag_server.py"
    priority: 63
    status: completed
    depends_on: [BG1]
    area: "api"
    problem_statement: "The FastAPI server entrypoint still decides when to send embedding dimensions inline, so app construction owns binding-specific dimension-delivery policy and related logging."
    root_cause_hypothesis: "The embedding setup block is still large, but the narrowest self-contained seam inside it is the `send_dimensions` policy: it depends only on the configured binding, the environment toggle, and whether the embedding callable accepts `embedding_dim`, so that policy never became its own helper module."
    definition_of_done:
      - "A dedicated helper module owns embedding `send_dimensions` policy and the related logging contract"
      - "`lightrag_server.py` uses that helper instead of deciding the dimension-delivery policy inline"
      - "The existing forced Jina/Gemini behavior and env-driven fallback behavior remain unchanged"
      - "Focused tests cover representative binding and signature combinations"
    validation_commands:
      - "python -m ruff check lightrag/api/embedding_dimension_policy.py lightrag/api/lightrag_server.py tests/test_embedding_dimension_policy.py"
      - "python -m pytest tests/test_embedding_dimension_policy.py -q"
    risk_level: "low"
    expected_files:
      - "lightrag/api/embedding_dimension_policy.py"
      - "lightrag/api/lightrag_server.py"
      - "tests/test_embedding_dimension_policy.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Queued after BG1 root-cause analysis showed the next narrow self-contained seam in `create_app()` is the embedding dimension-delivery policy block rather than the full embedding factory."
      - "Completed by introducing lightrag/api/embedding_dimension_policy.py for `send_dimensions` policy plus logging, then rewiring lightrag_server.py to apply that helper instead of deciding dimension delivery inline."
      - "Focused regression coverage lives in tests/test_embedding_dimension_policy.py."

  - id: BI1
    title: "Extract embedding factory from lightrag_server.py"
    priority: 64
    status: completed
    depends_on: [BH1]
    area: "api"
    problem_statement: "The FastAPI server entrypoint still defines the full embedding factory inline, so app construction owns provider-default resolution, provider-specific embedding dispatch, and EmbeddingFunc assembly."
    root_cause_hypothesis: "After `BH1`, the remaining embedding hotspot in `create_app()` was no longer the `send_dimensions` policy but the nested `create_optimized_embedding_function(...)` itself: it already formed a self-contained factory over `config_cache`, provider binding inputs, and `EmbeddingFunc`, yet it remained embedded in the app factory instead of becoming an authoritative helper module."
    definition_of_done:
      - "A dedicated helper module owns embedding provider-default resolution, optimized embedding dispatch, and EmbeddingFunc assembly"
      - "`lightrag_server.py` uses that helper instead of defining the embedding factory inline"
      - "The existing provider-default inheritance and representative provider dispatch behavior remain unchanged"
      - "Focused tests cover representative provider-default, cached-option, and modelscope initializer behavior"
    validation_commands:
      - "python -m ruff check lightrag/api/embedding_model_factory.py lightrag/api/lightrag_server.py tests/test_embedding_model_factory.py"
      - "python -m pytest tests/test_embedding_model_factory.py -q"
    risk_level: "medium"
    expected_files:
      - "lightrag/api/embedding_model_factory.py"
      - "lightrag/api/lightrag_server.py"
      - "tests/test_embedding_model_factory.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Queued after BH1 root-cause analysis showed the remaining embedding hotspot in `create_app()` is the nested embedding factory itself rather than another tiny policy branch."
      - "Completed by introducing lightrag/api/embedding_model_factory.py for provider-default resolution, provider-specific dispatch, and EmbeddingFunc assembly, then rewiring lightrag_server.py to call that helper."
      - "Focused regression coverage lives in tests/test_embedding_model_factory.py."

  - id: BJ1
    title: "Absorb embedding max-token-size logging into embedding_model_factory.py"
    priority: 65
    status: completed
    depends_on: [BI1]
    area: "api"
    problem_statement: "After `BI1`, the FastAPI server entrypoint still logs the embedding max-token-size source inline, so app construction still owns one remaining embedding assembly log contract."
    root_cause_hypothesis: "The `Embedding max_token_size` branch only depends on the built `EmbeddingFunc`, the embedding binding, and whether `args.embedding_token_limit` overrode the provider default, so this source-reporting rule belongs with embedding factory assembly instead of remaining in `create_app()`."
    definition_of_done:
      - "`embedding_model_factory.py` owns the embedding max-token-size source logging contract"
      - "`lightrag_server.py` no longer logs embedding max-token-size source inline"
      - "The existing env-var, provider-default, and disabled logging messages remain unchanged"
      - "Focused tests cover representative logging outcomes at the embedding-factory seam"
    validation_commands:
      - "python -m ruff check lightrag/api/embedding_model_factory.py lightrag/api/lightrag_server.py tests/test_embedding_model_factory.py"
      - "python -m pytest tests/test_embedding_model_factory.py -q"
    risk_level: "low"
    expected_files:
      - "lightrag/api/embedding_model_factory.py"
      - "lightrag/api/lightrag_server.py"
      - "tests/test_embedding_model_factory.py"
      - "docs/codex/TASK_QUEUE.yaml"
      - "docs/codex/DECISIONS.md"
    notes:
      - "Queued after BI1 root-cause analysis showed the remaining embedding assembly seam in `create_app()` is the max-token-size source logging block rather than the larger LightRAG constructor."
      - "Completed by moving the max-token-size source logging into lightrag/api/embedding_model_factory.py and removing the duplicate inline branch from lightrag_server.py."
      - "Focused regression coverage lives in tests/test_embedding_model_factory.py."
